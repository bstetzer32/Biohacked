# from .db import db

# scheme_set_rep = db.Table('scheme_set_reps',
#                           db.Column('scheme_id', db.Integer, db.ForeignKey(
#                             'scheme.id'), primary_key=True),
#                           db.Column('set_rep_id', db.Integer, db.ForeignKey(
#                             'set_rep.id'), primary_key=True),
#                           )
